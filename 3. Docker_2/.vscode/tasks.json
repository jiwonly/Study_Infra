{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Docker: Build and Run",
      "type": "shell",
      "command": "docker-compose down && docker-compose up --build -d",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
